[pytest]
testpaths = tests
markers =
    cibles: tests sur les chiffres cibles BottleNeck (necessite data/raw/bottleneck/)
    integration: tests qui necessitent une base DuckDB existante (lance run_pipeline.py avant)
addopts = -ra --strict-markers
filterwarnings =
    ignore::UserWarning:openpyxl.*
