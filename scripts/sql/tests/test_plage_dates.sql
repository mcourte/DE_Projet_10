-- Test de plage de dates : toutes les dates doivent etre dans la fenetre attendue.
-- TODO : definir la fenetre avec Stephane (ex : derniere annee).

SELECT
    -- TODO : COUNT(*) WHERE date_cmd NOT BETWEEN ... AND ...
    NULL AS nb_dates_hors_plage
;
